{
  "version": 2,
  "builds": [
    {
      "src": "frontend/dev-server/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "env": {
    "NITRO_PRESET": "vercel",
    "VITE_API_BASE_URL": {
      "description": "Base URL of the MediKiosk-Backend API. Leave empty for same-origin /api/v1, or set to your deployed backend URL (e.g., https://api.yourdomain.com)",
      "value": ""
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
